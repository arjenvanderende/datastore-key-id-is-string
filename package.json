{
  "name": "datastore-key-id-is-string",
  "version": "1.0.0",
  "description": "Repro case for @google-cloud/datastore issue where typeof key.id is string instead of number",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/datastore": "^1.1.0"
  }
}
